LOTS!

 - Test if the file system image exceeds the available space, in a way, where
   I am sure what is going on.
 - Find out if the file system image, should be loaded after the 0x40000 data
   in the flash.
   
